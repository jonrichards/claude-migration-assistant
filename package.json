{
  "name": "claude-migration-assistant",
  "version": "1.0.0",
  "description": "Claude Migration Assistant QA Testing",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "serve": "^14.2.5"
  }
}
